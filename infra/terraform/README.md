# infra/terraform

Terraform modules for cloud infrastructure provisioning.

> [!NOTE]
> **Not implemented.** No cloud resources are defined in this scaffold.
> Terraform state files and `.tfvars` files are excluded from version control via `.gitignore`.
> Do not add production credentials or cloud-specific secrets here.
