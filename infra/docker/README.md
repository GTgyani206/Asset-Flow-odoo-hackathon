# infra/docker

Docker-related configuration files for local development.

The primary Docker Compose file is at the repo root: `docker-compose.yml`.

Place any multi-stage Dockerfiles or environment-specific overrides here.
