# apps/web – tests

Playwright end-to-end tests and Vitest unit tests for `apps/web`.

- Unit tests for utility functions live alongside their source files as `*.test.ts`.
- Playwright e2e tests live here, targeting the running web app.

Run with: `pnpm test:e2e` from the repo root.
