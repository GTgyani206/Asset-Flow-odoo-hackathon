# src/lib

Utility functions, API client factories, and framework helpers for `apps/web`.

Examples:
- `api-client.ts` – typed fetch wrapper pointing at `NEXT_PUBLIC_API_URL`.
- `auth.ts` – session helpers.
- `date.ts` – UTC date formatting utilities.

Must not contain business logic. No domain rules here.
