# src/components

Shared UI components used across multiple pages in `apps/web`.

Import from `@assetflow/ui` for primitive components. Place page-specific or composed components here.

Do not add business logic to components. Data fetching belongs in `features/` or server components.
