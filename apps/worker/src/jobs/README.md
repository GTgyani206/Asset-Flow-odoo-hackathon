# apps/worker – jobs

Background job **definitions** and payload types.

Job handler implementations live in `consumers/`. Scheduler triggers live in `schedulers/`.

No job handlers are registered yet. Add a job here only when the corresponding business feature is implemented and tested.
