# scripts

Developer and CI utility scripts.

Examples to be added:
- `bootstrap-admin.sh` – Seeds the first admin user in a new environment.
- `check-env.sh` – Validates `.env` has all required variables from `.env.example`.
- `db-seed-dev.sh` – Seeds development data (never run in production).

All scripts must be idempotent and must not embed secrets.
