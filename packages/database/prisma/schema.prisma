// @ts-check
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Domain models are added incrementally as each module is implemented.
// Do not add models here until the corresponding module task is in progress.
