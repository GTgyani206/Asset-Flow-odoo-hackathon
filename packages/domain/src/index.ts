// Domain package entry point.
// Exports domain types, value objects, entity interfaces, and domain event types.
// No infrastructure, no framework imports, no side effects.

export const DOMAIN_VERSION = "0.0.1";
